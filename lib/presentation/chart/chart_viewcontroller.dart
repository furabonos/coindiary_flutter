import 'package:flutter/material.dart';

class ChartViewController extends StatelessWidget {
  const ChartViewController({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
    );
  }
}
